{
  "name": "@localterm/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/index.js",
  "dependencies": {
    "@localterm/control-plane": "workspace:*",
    "@localterm/shared": "workspace:*",
    "@mariozechner/pi-ai": "0.57.1"
  },
  "scripts": {
    "dev": "cross-env NODE_OPTIONS=--import=tsx LOCALTERM_RENDERER_URL=http://127.0.0.1:5173 electron ./src/main/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
