# File Browser React Widget

React + Tailwind webview widget for `file.browser`.

## Build

```bash
pnpm --filter @localterm/widget-file-react build
```

Output path:

- `extensions/builtin.workspace/widgets/file.browser/index.html`
- `extensions/builtin.workspace/widgets/file.browser/assets/*`
