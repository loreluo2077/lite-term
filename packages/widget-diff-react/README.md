# Diff Review React Widget

React + Tailwind + `react-diff-view` webview widget for `diff.review`.

## Build

```bash
pnpm --filter @localterm/widget-diff-react build
```

Output path:

- `extensions/builtin.workspace/widgets/diff.review/index.html`
- `extensions/builtin.workspace/widgets/diff.review/assets/*`
