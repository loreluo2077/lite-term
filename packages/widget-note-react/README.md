# Markdown React Widget

React + Tailwind webview widget for `note.markdown`.

## Build

```bash
pnpm --filter @localterm/widget-note-react build
```

Output path:

- `extensions/builtin.workspace/widgets/note.markdown/index.html`
- `extensions/builtin.workspace/widgets/note.markdown/assets/*`
